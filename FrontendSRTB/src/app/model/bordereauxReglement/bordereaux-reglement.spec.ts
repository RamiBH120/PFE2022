import { BordereauxReglement } from './bordereaux-reglement';

describe('BorderauxReglement', () => {
  it('should create an instance', () => {
    expect(new BordereauxReglement()).toBeTruthy();
  });
});
